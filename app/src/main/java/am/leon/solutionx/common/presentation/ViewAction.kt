package am.leon.solutionx.common.presentation

/**
 * Immutable object which represent an view's intent.
 *
 * flows from UI to ViewModel. This is the only legal way to tell something to ViewModel from UI screen.
 */
interface ViewAction