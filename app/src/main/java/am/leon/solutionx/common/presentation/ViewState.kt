package am.leon.solutionx.common.presentation

/**
 * Immutable object which contains all the required information to render a [ViewState].
 *
 * flows from ViewModel to UI. It describes what screen state should be displayed right now.
 */
interface ViewState